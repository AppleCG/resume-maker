# resume-maker 技能操作规范

> 此文件由技能在第零步自动读取。记录当前用户的档案架构、路径约定和操作约束。

## 文件定义（核心）

### self_profile.md — 仅个人信息，不含任何项目/经历

只记录：基本信息、求职意向、教育背景、技能、证书、自我评价。

**禁止**放入工作经历、竞赛经历、社会实践、个人项目。

### resume_base.md — 个人信息 + 项目索引

包含 `self_profile.md` 中个人信息的摘要版，加上项目经验。**项目只做索引**，以表格引用 `resources/projects/` 下的文件，不展开内联内容。

### resources/projects/ — 每段经历的详细文件

工作经历、竞赛经历、社会实践、个人项目——全部统一为"项目"概念，每种一段一个 `.md` 文件。这是经历的**唯一权威来源**。

---

## 项目文件覆盖范围

工作经历、竞赛经历、社会实践、个人项目——**全部**拆分为独立文件存入 `resources/projects/`。

**How to apply:** 从 PDF 或其他来源提取简历后，每一段经历（无论类型）都创建独立项目文件。

---

## 示例项目清理

技能自带的示例项目文件如果与用户实际背景不符，必须先删除。

**How to apply:** 首次建立档案时，检查 `projects/` 并清除非用户本人的项目文件。

---

## 信息采集流程

1. 完整提取现有简历（PDF/文本等）的全部内容
2. 用表格汇总展示给用户确认
3. 逐项询问缺失信息（项目、英语能力、个人链接等）
4. 每采集完一项立即写入对应文件

---

## 母版结构约束

`resume_base.md` 中：
- 项目部分**只有** `## 项目经验` 下的分类表格索引
- 不得出现内联展开的工作经历/竞赛/社会实践章节
- `self_profile.md` 同样不得出现经历内联内容

**Why:** 经历以 `projects/*.md` 为唯一权威来源，避免多头维护导致不一致。

**How to apply:** 每次更新后自检两个文件，确保经历只存在于 `projects/` 目录中。

---

## 模板文件

- `resources/template_word.md` — Word/WPS 简历排版指南，用于生成 .docx 简历
- `resources/template_html.md` — HTML 简历模板（含 CSS 样式骨架），用于生成 HTML 简历
